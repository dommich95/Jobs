<div class="form"><%# Sign in page %>
    <h1>Log In</h1>
    <% if flash[:error].present? %> <%# check sif deatils were correct %>
    <% end%>

    <%= form_tag login_path do %><%# which deatils are required for log in %>
        <%= label_tag :email %>
        <%= email_field_tag :email %>
        <%= label_tag :password %>
        <%= password_field_tag :password %>
        <%= submit_tag "Sign in", :class => 'submit' %>
    <% end %>
</div>

