<h1>All users</h1><%# For admin only, shows the users contact details %>

<h1><%= @users.count %></h1> <%# shows the admin how many user the platform has  %>

<% @users.each do |user| %> <%# puts each user in its own box with details like name and email %>
    <ul>
        <li> Name: <%= user.name %></li>
        <li>Surname <%= user.family %></li>
        <li>Email: <%= user.email %></li>
    </ul>
    
 <% end %>

